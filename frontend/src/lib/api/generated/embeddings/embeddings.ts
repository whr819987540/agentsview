/**
 * Generated by Orval. Do not edit manually.
 */
import type {
  EmbeddingsBuildRequest,
  EmbeddingsBuildResponse,
  EmbeddingsGenerationActionRequest,
  EmbeddingsGenerationsResponse,
  GetApiV1EmbeddingsGenerationsParams,
  GetApiV1EmbeddingsStatusParams,
  PostApiV1EmbeddingsGenerationsByIdActivateParams,
  PostApiV1EmbeddingsGenerationsByIdActivatePathParameters,
  PostApiV1EmbeddingsGenerationsByIdRetireParams,
  PostApiV1EmbeddingsGenerationsByIdRetirePathParameters,
  VectorBuildStatus,
} from "../models";

import { orvalFetch } from "../../runtime.ts";

export const getPostApiV1EmbeddingsBuildUrl = () => {
  return `/api/v1/embeddings/build`;
};

/**
 * @summary Start an embeddings build
 */
export const postApiV1EmbeddingsBuild = async (
  embeddingsBuildRequest: EmbeddingsBuildRequest,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<EmbeddingsBuildResponse> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<string | readonly string[] | undefined>(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
  return orvalFetch<EmbeddingsBuildResponse>(getPostApiV1EmbeddingsBuildUrl(), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...getHeaders(options?.headers) },
    body: JSON.stringify(embeddingsBuildRequest),
  });
};

export const getGetApiV1EmbeddingsGenerationsUrl = (
  params?: GetApiV1EmbeddingsGenerationsParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/v1/embeddings/generations?${stringifiedParams}`
    : `/api/v1/embeddings/generations`;
};

/**
 * @summary List embedding generations
 */
export const getApiV1EmbeddingsGenerations = async (
  params?: GetApiV1EmbeddingsGenerationsParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<EmbeddingsGenerationsResponse> => {
  return orvalFetch<EmbeddingsGenerationsResponse>(getGetApiV1EmbeddingsGenerationsUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getPostApiV1EmbeddingsGenerationsByIdActivateUrl = (
  { id }: PostApiV1EmbeddingsGenerationsByIdActivatePathParameters,
  params?: PostApiV1EmbeddingsGenerationsByIdActivateParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/v1/embeddings/generations/${encodeURIComponent(String(id))}/activate?${stringifiedParams}`
    : `/api/v1/embeddings/generations/${encodeURIComponent(String(id))}/activate`;
};

/**
 * @summary Activate an embedding generation
 */
export const postApiV1EmbeddingsGenerationsByIdActivate = async (
  { id }: PostApiV1EmbeddingsGenerationsByIdActivatePathParameters,
  embeddingsGenerationActionRequest: EmbeddingsGenerationActionRequest,
  params?: PostApiV1EmbeddingsGenerationsByIdActivateParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<void> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<string | readonly string[] | undefined>(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
  return orvalFetch<void>(getPostApiV1EmbeddingsGenerationsByIdActivateUrl({ id }, params), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...getHeaders(options?.headers) },
    body: JSON.stringify(embeddingsGenerationActionRequest),
  });
};

export const getPostApiV1EmbeddingsGenerationsByIdRetireUrl = (
  { id }: PostApiV1EmbeddingsGenerationsByIdRetirePathParameters,
  params?: PostApiV1EmbeddingsGenerationsByIdRetireParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/v1/embeddings/generations/${encodeURIComponent(String(id))}/retire?${stringifiedParams}`
    : `/api/v1/embeddings/generations/${encodeURIComponent(String(id))}/retire`;
};

/**
 * @summary Retire an embedding generation
 */
export const postApiV1EmbeddingsGenerationsByIdRetire = async (
  { id }: PostApiV1EmbeddingsGenerationsByIdRetirePathParameters,
  embeddingsGenerationActionRequest: EmbeddingsGenerationActionRequest,
  params?: PostApiV1EmbeddingsGenerationsByIdRetireParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<void> => {
  const getHeaders = (
    h?: NonNullable<RequestInit["headers"]>,
  ): Record<string, string | readonly string[]> => {
    if (!h) return {};
    if (h instanceof Headers) return Object.fromEntries(h.entries());
    if (Symbol.iterator in h) {
      return Object.fromEntries(
        Array.from(
          h as Iterable<Iterable<string>>,
          (entry) => Array.from(entry) as [string, string],
        ),
      );
    }
    const headers: Record<string, string | readonly string[]> = {};
    for (const [name, value] of Object.entries<string | readonly string[] | undefined>(h)) {
      if (value !== undefined) headers[name] = value;
    }
    return headers;
  };
  return orvalFetch<void>(getPostApiV1EmbeddingsGenerationsByIdRetireUrl({ id }, params), {
    ...options,
    method: "POST",
    headers: { "Content-Type": "application/json", ...getHeaders(options?.headers) },
    body: JSON.stringify(embeddingsGenerationActionRequest),
  });
};

export const getGetApiV1EmbeddingsStatusUrl = (params?: GetApiV1EmbeddingsStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/v1/embeddings/status?${stringifiedParams}`
    : `/api/v1/embeddings/status`;
};

/**
 * @summary Embeddings build status
 */
export const getApiV1EmbeddingsStatus = async (
  params?: GetApiV1EmbeddingsStatusParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<VectorBuildStatus> => {
  return orvalFetch<VectorBuildStatus>(getGetApiV1EmbeddingsStatusUrl(params), {
    ...options,
    method: "GET",
  });
};
