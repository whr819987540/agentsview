/**
 * Generated by Orval. Do not edit manually.
 */
import type {
  GetApiV1SearchContentParams,
  GetApiV1SearchParams,
  SearchResponse,
  ServiceContentSearchResult,
} from "../models";

import { orvalFetch } from "../../runtime.ts";

export const getGetApiV1SearchUrl = (params: GetApiV1SearchParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v1/search?${stringifiedParams}` : `/api/v1/search`;
};

/**
 * @summary Search sessions
 */
export const getApiV1Search = async (
  params: GetApiV1SearchParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<SearchResponse> => {
  return orvalFetch<SearchResponse>(getGetApiV1SearchUrl(params), {
    ...options,
    method: "GET",
  });
};

export const getGetApiV1SearchContentUrl = (params: GetApiV1SearchContentParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["exclude_session"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/v1/search/content?${stringifiedParams}`
    : `/api/v1/search/content`;
};

/**
 * @summary Search session content
 */
export const getApiV1SearchContent = async (
  params: GetApiV1SearchContentParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<ServiceContentSearchResult> => {
  return orvalFetch<ServiceContentSearchResult>(getGetApiV1SearchContentUrl(params), {
    ...options,
    method: "GET",
  });
};
