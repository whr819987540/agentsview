/**
 * Generated by Orval. Do not edit manually.
 */
import type { DbTrendsTermsResponse, GetApiV1TrendsTermsParams } from "../models";

import { orvalFetch } from "../../runtime.ts";

export const getGetApiV1TrendsTermsUrl = (params?: GetApiV1TrendsTermsParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    const explodeParameters = ["term"];

    if (Array.isArray(value) && explodeParameters.includes(key)) {
      value.forEach((v) => {
        normalizedParams.append(key, v === null ? "null" : String(v));
      });
      return;
    }

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? "null" : String(value));
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/api/v1/trends/terms?${stringifiedParams}`
    : `/api/v1/trends/terms`;
};

/**
 * @summary Get trend terms
 */
export const getApiV1TrendsTerms = async (
  params?: GetApiV1TrendsTermsParams,
  options?: Parameters<typeof orvalFetch>[1],
): Promise<DbTrendsTermsResponse> => {
  return orvalFetch<DbTrendsTermsResponse>(getGetApiV1TrendsTermsUrl(params), {
    ...options,
    method: "GET",
  });
};
